import { SearchOutlined } from "@mui/icons-material";
import { IconButton } from "@mui/material";

export const Search = () => {
  return (
    <>
      <IconButton>
        <SearchOutlined />
      </IconButton>
    </>
  );
};
